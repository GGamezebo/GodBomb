---
description: Yandex Games — preset, SDK, требования модерации
globs: src/common/platform/**,addons/yandex_games/**,export_presets.cfg,project.godot
alwaysApply: false
---

# Yandex Games

**Preset:** `Yandex` → `bin/yandex/`, feature `yandex`, HTML shell `addons/yandex_games/templates/yandex_template.html`. Отдельно от `Web` и `Android`.

**Интеграция:** plugin `Yandex Games SDK` + autoload `YandexGames` + `PlatformServices`. Хуки **только** при `OS.has_feature("yandex")` — Android/Play и plain Web не трогаем.

**Граница слоёв:** `LocaleService` / `LocaleCatalog` / FSM / HUD — платформенно-нейтральны. Портальный `i18n.lang`, ads, `game_ready` — только через `PlatformServices`. Не тянуть `YandexGames` и `OS.has_feature("yandex")` в common/locale или contexts.

**Lifecycle (yandex):** `game_ready` после init · `gameplay_start` на `play` · `gameplay_stop` иначе · interstitial при выходе в меню после `result` · **2.14** `i18n.lang` → `LocaleService.set_locale` на старте.
